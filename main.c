#include<stdio.h>
#include"calc.h"

int main(){
	printf("Sum is %d\n",add(10,20));
        printf("subtraction is %d\n",sub(10,20));
        printf("multiplication is %d\n",mul(10,20));

	return 0;
}
